// Example: Toggle full blog content
document.querySelectorAll('.btn-primary').forEach(button => {
    button.addEventListener('click', (e) => {
      e.preventDefault();
      alert('This is where the full blog post would appear!');
    });
  });